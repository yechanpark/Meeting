<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.meeting.mapper.userMapper">  <!-- namespace로 Mapper를 확인한다.  -->
   <insert id="insertUser">
   insert into users (username,password,displayname) values (#{username},#{password},#{displayname})
   </insert>
  
  
  </mapper>